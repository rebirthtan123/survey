<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Q3Graph"></activity>
        <activity android:name=".Q2Graph" />
        <activity android:name=".Q1Graph" />
        <activity android:name=".graph" />
        <activity android:name=".view" />
        <activity android:name=".survey2" />
        <activity android:name=".survey" />
        <activity android:name=".register" />
        <activity android:name=".login" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".about" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>